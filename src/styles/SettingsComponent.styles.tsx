import {StyleSheet} from 'react-native';

export const SettingsComponentStyles = StyleSheet.create({
  AccordionContainer: {
    backgroundColor: 'white',
    marginBottom: 20,
    width: 60,
  },
  AccordionIcon: {
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
  }
});
